name: cicd
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print to console
        run: echo Hello
      - name: Print few raws
        run: |
          echo First
          echo Second
